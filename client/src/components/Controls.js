import React from 'react';

const Controls = () => {

  // const logKey = (e) => {
  //   console.log(e.key)
  //   Body.setAngularVelocity(ship, 0.1)
  // }
  // document.addEventListener('keypress', logKey);

  return null;

}

export default Controls;